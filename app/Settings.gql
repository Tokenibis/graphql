query Settings($id: ID!) {
    ibisUser(id: $id) {
	username
	description
	email
	visibilityDonation
	visibilityTransaction
	notifier {
	    id
	    emailFollow
	    emailDonation
	    emailTransaction
	    emailComment
	    emailMention
	    emailDeposit
	}
    }
}
