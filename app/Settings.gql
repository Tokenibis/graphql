query Settings($id: ID!) {
    user(id: $id) {
	username
	description
	email
	notifier {
	    id
	    emailFollow
	    emailDonation
	    emailReward
	    emailComment
	    emailMention
	    emailDeposit
	}
    }
}
