query EventList($self: String $search: String $byUser: String $byFollowing: String $bookmarkBy: String $rsvpBy: String $beginDate: String $orderBy: String $first: Int! $after: String) {
    allEvents(search: $search byUser: $byUser byFollowing: $byFollowing bookmarkBy: $bookmarkBy rsvpBy: $rsvpBy beginDate: $beginDate orderBy: $orderBy first: $first after: $after) {
	edges {
  	    node {
		id
  		title
		image
  		description
  		created
		date
		likeCount
		rsvpCount
		user {
		    id
		    name
		    username
		    avatar
		    organization {
			id
			category {
			    id
			}
		    }
		}
		hasLiked: like(id: $self) {
		    edges {
			node {
			    id
			}
		    }
		}
		hasBookmarked: bookmark(id: $self) {
		    edges {
			node {
			    id
			}
		    }
		}
		hasRsvp: rsvp(id: $self) {
		    edges {
			node {
			    id
			}
		    }
		}
	    }
	    cursor
	}
	pageInfo {
	    hasNextPage
	}
    }
}
