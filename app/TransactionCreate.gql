mutation TransactionCreate($user: ID! $target: ID! $amount: Int! $description: String! $private: Boolean) {
    createTransaction(user: $user target: $target amount: $amount description: $description private: $private) {
	transaction {
	    id
	}
    }
}
