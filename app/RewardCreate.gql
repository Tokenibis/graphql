mutation RewardCreate($user: ID! $target: ID! $amount: Int! $description: String! $private: Boolean) {
    createReward(user: $user target: $target amount: $amount description: $description private: $private) {
	reward {
	    id
	}
    }
}
