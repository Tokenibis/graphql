mutation RewardCreate($user: ID! $target: ID! $amount: Int! $description: String!) {
    createReward(user: $user target: $target amount: $amount description: $description) {
	reward {
	    id
	}
    }
}
