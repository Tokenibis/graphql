query Nonprofit($id: ID! $self: String){
    nonprofit(id: $id){
	id
	followerCount
	description
	title
	link
	avatar
	fundraised
	donationFromCount
	newsCount
	eventCount
	isFollowing: follower(id: $self) {
	    edges {
		node {
		    id
		}
	    }
	}
    }
}
