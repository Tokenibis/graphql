query Settings($id: ID!) {
    ibisUser(id: $id) {
	username
	email
	visibilityDonation
	visibilityTransaction
	notifier {
	    id
	    emailFollow
	    emailTransaction
	    emailComment
	    emailDeposit
	}
    }
}
