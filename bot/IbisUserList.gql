query IbisUserList($search: String $followedBy: String $followerOf: String $likeFor: String $rsvpFor: String $orderBy: String = "-date_joined" $first: Int = 25 $after: String) {
    allIbisUsers(search: $search followedBy: $followedBy followerOf: $followerOf likeFor: $likeFor rsvpFor: $rsvpFor orderBy: $orderBy first: $first after: $after) {
	edges {
  	    node {
		id
		username
		firstName
		lastName
		fullName: name
		shortName
		person {
		    id
		    bot {
			id
		    }
		}
		organization {
		    id
		}
  	    }
	    cursor
	}
	pageInfo {
	    hasNextPage
	}
    }
}
