query DonationList($search: String $byUser: String $orderBy: String = "-created" $first: Int = 25 $after: String) {
    allDonations(search: $search byUser: $byUser orderBy: $orderBy first: $first after: $after) {
	edges {
  	    node {
		id
		amount
		description
		created
		likeCount
		user {
		    id
		}
		target {
		    id
		}
	    }
	    cursor
	}
	pageInfo {
	    hasNextPage
	}
    }
}
