query RewardList($search: String $user: String $orderBy: String = "-created" $first: Int = 25 $after: String) {
    allRewards(search: $search user: $user orderBy: $orderBy first: $first after: $after) {
	edges {
  	    node {
		id
		amount
		description
		created
		likeCount
		user {
		    id
		}
		target {
		    id
		}
	    }
	    cursor
	}
	pageInfo {
	    hasNextPage
	}
    }
}
