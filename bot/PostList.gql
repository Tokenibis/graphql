query PostList($search: String $user: String $orderBy: String = "-created" $first: Int = 25 $after: String) {
    allPosts(search: $search user: $user orderBy: $orderBy first: $first after: $after) {
	edges {
  	    node {
		id
		title
		description
		created
		likeCount
		user {
		    id
		}
	    }
	    cursor
	}
	pageInfo {
	    hasNextPage
	}
    }
}
