mutation ActivityUpdate($id: ID! $user: ID $title: String $description: String $active: Boolean $rewardMin: Int $rewardRange: Int) {
    updateActivity(id: $id user: $user title: $title description: $description active: $active rewardMin: $rewardMin rewardRange: $rewardRange) {
	activity {
	    id
	}
    }
}
